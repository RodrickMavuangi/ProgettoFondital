@inject LavorazioneClient httpClient
@inject IStringLocalizer<App> localizer

<EditForm Model="NuovaLavorazione" OnValidSubmit="SalvaLavorazione">
    <DataAnnotationsValidator />

    <div>@ErrorMessage</div>
    <div class="form-field">
        <label>@localizer["NomeItaliano"]</label>
        <div>
            <InputText @bind-Value="NuovaLavorazione.NomeItaliano"></InputText>
            <ValidationMessage For="() => NuovaLavorazione.NomeItaliano" />
        </div>
    </div>
    <div class="form-field">
        <label>@localizer["NomeRusso"]</label>
        <div>
            <InputText @bind-Value="NuovaLavorazione.NomeRusso"></InputText>
            <ValidationMessage For="() => NuovaLavorazione.NomeRusso" />
        </div>
    </div>

    @*<button class="btn btn-secondary mr-auto" @onclick="OnClose">@localizer["Cancella"]</button>*@
    <button type="submit" class="btn btn-success ml-auto" disabled="@isSubmitting">@localizer["Salva"]</button>

</EditForm>