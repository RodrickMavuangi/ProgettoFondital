@inject IJSRuntime JSRuntime
@inject IStringLocalizer<App> localizer
@inject NavigationManager NavigationManager

<AuthorizeView Roles="Direzione">
    <Authorized>
        <TelerikMenu Data="@MenuAnagrafiche"
                     Class="nav nav-pills col-12 col-lg-auto mb-2 mb-md-0"
                     OnClick="@((MenuItem item) => OnClickHandler(item))">
            <ItemTemplate Context="item">
                <NavLink href="@item.Url"
                         target="@(IsInternalPage(item.Url) ? "" : "_blank")"
                         class="navLink"
                         ActiveClass="active"
                         Match="@(item.Url == "/" ? NavLinkMatch.All : NavLinkMatch.Prefix)">
                    @item.Text
                </NavLink>
            </ItemTemplate>
        </TelerikMenu>
    </Authorized>
</AuthorizeView>
<AuthorizeView Roles="Direzione,Service Partner">
    <Authorized>
        <TelerikMenu Data="@MenuRapporti"
                     Class="nav nav-pills col-12 col-lg-auto mb-2 mb-md-0"
                     OnClick="@((MenuItem item) => OnClickHandler(item))">
            <ItemTemplate Context="item">
                <NavLink href="@item.Url"
                         target="@(IsInternalPage(item.Url) ? "" : "_blank")"
                         class="navLink"
                         ActiveClass="active"
                         Match="@(item.Url == "/" ? NavLinkMatch.All : NavLinkMatch.Prefix)">
                    @item.Text
                </NavLink>
            </ItemTemplate>
        </TelerikMenu>
    </Authorized>
</AuthorizeView>
<AuthorizeView Roles="Direzione">
    <Authorized>
        <TelerikMenu Data="@MenuConfigurazioni"
                     Class="nav nav-pills col-12 col-lg-auto mb-2 mb-md-0"
                     OnClick="@((MenuItem item) => OnClickHandler(item))">
            <ItemTemplate Context="item">
                <NavLink href="@item.Url"
                         target="@(IsInternalPage(item.Url) ? "" : "_blank")"
                         class="navLink"
                         ActiveClass="active"
                         Match="@(item.Url == "/" ? NavLinkMatch.All : NavLinkMatch.Prefix)">
                    @item.Text
                </NavLink>
            </ItemTemplate>
        </TelerikMenu>
    </Authorized>
</AuthorizeView>